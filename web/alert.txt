[css]:
复合属性连写不需要逗号 如font border等
并集选择器要加逗号 否则是子代选择器
标签选择器的标签名前不要加点

[js]:
<script src=...>标签内的代码先于src的外部脚本执行,
如果标签内的代码使用了外部脚本中的函数或变量 程序会无法运行

[vue]:
使用v-bind绑定属性名时 属性名一定要小写
对响应式数据操作一定要是使用value属性

[prettier]:
出现语法错误时 插件不会工作 检查元素标签是否关闭 或者引号是否配对导致标签没有关闭